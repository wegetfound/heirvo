// Injected by vite.config.ts at build time (see `define`). These let the UI
// display the exact commit + build timestamp it was compiled from.
declare const __BUILD_HASH__: string;
declare const __BUILD_TIME__: string;
